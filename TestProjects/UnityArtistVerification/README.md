# UnityArtistCLI verification fixture

This fixture is intentionally minimal and is used for host-side project detection and package path validation. Direct Editor/Pipeline tests use the same package with the selected Unity version from the formal support matrix.
