# MCP Client Templates

Unityの`Window > MCP for Unity`による自動設定を優先してください。ここにあるTemplateは、自動検出できないHTTP対応Client向けのFallback例です。

`<UNITY_MCP_HTTP_URL>`をBridge UIに表示されたEndpointへ置換します。ClientごとにSchemaが変わるため、使用中Clientの公式仕様を確認してください。

`recommended-readonly-allowlist.json`は接続設定ではなく、最初に公開するToolの推奨一覧です。
